import React from "react";

export default function ReceiptPlaceholder() {
    return (
        <div className="w-full border border-dashed border-stone-300 bg-stone-50 flex items-center justify-center py-10 text-stone-400 text-sm">
            Ảnh hoá đơn (hiển thị đầy đủ, không cắt)
        </div>
    );
}
